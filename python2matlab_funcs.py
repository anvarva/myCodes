## Numpy function name changing to that of matlab
import matplotlib.pyplot as plt
import numpy as np
import scipy.integrate as integrate

#numpy
pi = np.pi
sin = np.sin
cos = np.cos
tan = np.tan
sqrt = np.sqrt
rem = np.remainder
atan = np.arctan 
linspace = np.arange


#matplotlib

plot = plt.plot
ylabel = plt.ylabel
xlabel = plt.xlabel
show = plt.show
subplots = plt.subplots
subplots_adjust = plt.subplots_adjust
axes = plt.axes

#integrate
quad = integrate.quad